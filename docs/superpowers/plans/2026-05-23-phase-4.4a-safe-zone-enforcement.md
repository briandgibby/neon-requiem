# Phase 4.4A Plan: Safe-Zone Enforcement Foundation

---
phase: 4.4A
status: complete
depends_on:
  - 4.3
enables:
  - elite-mob-spawn
  - mob-aggro-follow
  - world-events
  - friendly-event-npc-support
domains:
  - world
  - combat
  - engine
systems:
  - safe-zone-policy
  - security-patrol
  - combat-reinforcement
---

**Date:** 2026-05-23
**Status:** Complete
**Reference:** Phase 4.3 follow-on: safe-zone mob AI enforcement

---

## Goal

Establish the canonical safe-zone policy before adding richer mob aggro, follow, elite spawn, and event systems.

Safe zones should suppress automated hostile NPC behavior by default. Event systems can later override this behavior by flipping room-level flags for the affected event area.

Canonical rule:

```ts
effectiveSafeZone = room.isSafeZone && !room.safeZoneOverrideActive
```

---

## Decisions

1. Phase 4.4A blocks **automated hostile behavior only**.
2. Player-initiated combat, PvP rules, reward accounting, event recruitment, and faction allegiance behavior are out of scope.
3. `safeZoneOverrideActive` is the blunt room-level event override for now.
4. The safe-zone rule belongs in the world domain because it governs zone-specific behavior.
5. Use two enforcement checks:
   - Alarm triggering should be blocked in effective safe zones.
   - Reinforcement spawning should also be blocked in effective safe zones.
6. Blocked automated alarms should be clean no-ops with explicit results, leaving room cleanliness unchanged.

---

## Non-Goals

- No full mob aggro/follow pathfinding.
- No elite mob spawn logic.
- No body-guarding mechanics for jacked-in deckers.
- No event scheduler or `WorldEventService`.
- No participation/reward tracking.
- No friendly/faction-aware NPC combat behavior.
- No PvP permission model changes.

---

## Proposed Implementation

### 1. Add World-Domain Safe-Zone Policy — COMPLETE

Files:

- `src/domains/world/world.service.ts`
- `src/domains/world/world.types.ts`
- `tests/world/world.service.test.ts`

Add a pure policy helper for callers that already have a room record:

```ts
export function isEffectiveSafeZone(room: Pick<RoomRecord, 'isSafeZone' | 'safeZoneOverrideActive'>): boolean {
  return room.isSafeZone && !room.safeZoneOverrideActive;
}
```

Add a service lookup for systems that only have a room id:

```ts
async isEffectiveSafeZone(roomId: string): Promise<boolean>
```

This keeps combat and future event systems from duplicating room flag logic.

Implementation notes:

- `isEffectiveSafeZone(room)` was added as the canonical pure helper.
- `WorldService.isEffectiveSafeZone(roomId)` was added for callers with only a room id.
- Missing rooms throw `NotFoundError('Room')`, matching existing `WorldService` lookup behavior.
- Covered by `tests/world/world.service.test.ts`.

### 2. Block Automated Alarm Triggering — COMPLETE

Files:

- `src/domains/combat/combat.service.ts`
- `src/domains/combat/combat.types.ts`
- `src/engine/security-patrol.ts`
- `tests/combat/combat.service.test.ts`
- `tests/engine/security-patrol.test.ts`

Update `CombatService.triggerSecurityAlarm(roomId)` to consult the world-domain policy before creating or mutating a combat session.

Recommended return shape:

```ts
type SecurityAlarmResult =
  | { triggered: true }
  | { triggered: false; reason: 'safe_zone' };
```

When blocked:

- Do not create a combat session.
- Do not set `alarmState = RED`.
- Do not set `backupCalled = true`.
- Do not alter room cleanliness.

`SecurityPatrol` can log that a dirty safe-zone room was skipped, but should not otherwise mutate combat state.

Implementation notes:

- `SecurityAlarmResult` was added:
  - `{ triggered: true }`
  - `{ triggered: false; reason: 'safe_zone' }`
- `CombatService.triggerSecurityAlarm(roomId)` now owns the alarm-blocking behavior and checks the world-domain safe-zone policy before `getOrCreateEcsSession`.
- Effective safe zones do not create ECS combat sessions, set `alarmState = RED`, set `backupCalled = true`, set `turnsUntilReinforcements`, or alter room cleanliness.
- Missing rooms propagate the world-domain `NotFoundError('Room')` policy and do not create phantom alarm sessions.
- Safe-zone override and non-safe-zone rooms still trigger RED alert.
- `SecurityPatrol` now reacts to the explicit result, logs safe-zone skips, and reports triggered/skipped counts without knowing safe-zone flags.
- Covered by `tests/combat/combat.service.test.ts` and `tests/engine/security-patrol.test.ts`.

### 3. Block Reinforcement Spawning — COMPLETE

Files:

- `src/engine/ecs/systems/combat-reinforcement-system.ts`
- `src/server.ts`
- `tests/engine/combat-system.test.ts`

Inject the world-domain safe-zone policy into `CombatReinforcementSystem`.

Before spawning reinforcements for a session with `backupCalled && turnsUntilReinforcements === 0`, check the session room:

- If `effectiveSafeZone` is true, do not spawn reinforcements.
- Clear or defer the pending reinforcement state intentionally. Preferred behavior: clear `turnsUntilReinforcements` to avoid retry spam while the room remains protected.
- Preserve the session otherwise.

This second check protects against stale/preexisting combat sessions and event flags changing while backup is pending.

Implementation notes:

- `CombatReinforcementSystem` depends on a narrow `SafeZonePolicy` contract rather than the concrete world service.
- The system checks the policy immediately before spawning pending reinforcements.
- Effective safe zones clear `turnsUntilReinforcements` without spawning, preventing retries on later ticks.
- Override and non-safe-zone rooms retain the existing spawn behavior.
- `src/server.ts` supplies `WorldService` as the policy implementation.

---

## Test Plan

### World Policy

- [x] `isEffectiveSafeZone` returns true when `isSafeZone = true` and `safeZoneOverrideActive = false`.
- [x] Returns false when the room is not a safe zone.
- [x] Returns false when override is active.
- [x] `WorldService.isEffectiveSafeZone(roomId)` throws `NotFoundError('Room')` for missing rooms.

### Alarm Triggering

- [x] `triggerSecurityAlarm` does not create a combat session for an effective safe zone.
- [x] `triggerSecurityAlarm` still triggers RED alert when override is active.
- [x] `triggerSecurityAlarm` still triggers RED alert in non-safe-zone rooms.
- [x] `SecurityPatrol` skips effective safe zones.

### Reinforcement Spawning

- [x] Reinforcements do not spawn into effective safe zones.
- [x] Reinforcements do spawn when override is active.
- [x] Reinforcements do spawn in non-safe-zone rooms.
- [x] Safe-zone blocked reinforcement does not repeatedly spawn or retry every tick.

### Current Verification

Verified after items 1 and 2 (2026-05-23):

```bash
PATH=/home/bdgibby/.local/share/fnm/node-versions/v22.22.2/installation/bin:$PATH npm run build
PATH=/home/bdgibby/.local/share/fnm/node-versions/v22.22.2/installation/bin:$PATH npm test -- --silent
```

Result:

- `npm run build`: passes.
- `npm test -- --silent`: passes, **26 suites / 156 tests**.

Verified reinforcement completion (2026-07-12):

```bash
npx jest --runInBand tests/engine/combat-system.test.ts tests/world/world.service.test.ts
npm run build
```

Result:

- Focused tests pass, **2 suites / 22 tests**.
- `npm run build`: passes.

Verified final backend result (2026-07-12):

```bash
npm test -- --silent
npm run build
```

Result:

- `npm test -- --silent`: passes, **26 suites / 163 tests**.
- `npm run build`: passes.

### Verification Commands

```bash
npm run build
npm test -- --silent
```

Frontend verification is not required unless shared types or UI-visible behavior changes.

---

## Deferred Event Behavior

Future hostile safe-zone events may intentionally enable aggressive action in normally protected areas by setting `safeZoneOverrideActive = true` on affected rooms.

Examples:

- Insect spirit invasion in a public district.
- Corporate raid spilling into a civic safe zone.
- Law-enforcement containment event where players are recruited to help.

For those events, alarm-like escalation should not automatically mean hostile law/security NPCs attack players. Event-specific behavior may instead spawn friendly or allied security NPCs that fight alongside recruited players against event enemies.

Deferred mechanics:

- Event lifecycle that flips `safeZoneOverrideActive` on start/end.
- Event-specific faction and allegiance rules.
- Friendly NPC support behavior.
- Player recruitment.
- Participation and reward tracking.
- Event cleanup and restoration guarantees.

---

## Documentation Practice Going Forward

Every phase should have a unique, explicitly referenceable plan document under `docs/superpowers/plans/`.

Overall build progress should be summarized in handoffs and progress reports instead of maintained in a single rolling master plan document.
