# Phase 4.4B Plan: Physical Mob AI Targeting

---
phase: 4.4B
status: slice-3-complete
depends_on:
  - 4.4A
enables:
  - mob-aggro-follow
  - decker-body-guarding
  - elite-mob-spawn
domains:
  - combat
  - world
  - engine
systems:
  - mob-ai
  - safe-zone-policy
  - move-dispatcher
---

**Date:** 2026-07-12
**Status:** Slice 3 complete
**Reference:** Phase 4.3 follow-on: mob aggro/follow system with safe-zone boundary enforcement

---

## Goal

Give hostile physical NPCs a real combat behavior loop while preserving Phase 4.4A safe-zone protection.

This phase starts with in-room targeting, attacks, one-room pursuit, and bounded multi-room pursuit. Elite RED-alert spawning and body-guarding are separate vertical slices.

---

## Slice 1 — In-Room Physical Mob AI — COMPLETE

Implemented:

- `MobAiSystem` heartbeat subscriber.
- Hostile NPCs with `AiComponent.state = 'hostile'` can select valid player targets in the same physical room.
- Effective safe zones no-op automated hostile mob attacks.
- Jacked-in deckers are targetable through `DeckerComponent.physicalRoomId` instead of their matrix `PositionComponent.roomId`.
- Mob attacks resolve through the existing `MoveDispatcher` and `AttackExecutor`.
- Reinforcement and mission-target mobs are created with hostile AI state so production-spawned hostile NPCs participate in the loop.
- AP-starved hostile mobs enter recovery so `CombatTickSystem` can refill AP and allow later attacks.
- Server composition subscribes `MobAiSystem` with `WorldService` as the safe-zone policy.
- Shared `SafeZonePolicy` contract lives in the World domain and is consumed by alarm, reinforcement, and mob AI systems.

Deferred:

- Room-to-room chase/follow behavior.
- Safe-zone boundary drop behavior during pursuit.
- Body-guarding/interception for jacked-in deckers.
- RED-alert elite mob spawning.

---

## Slice 2 — One-Room Pursuit and Safe-Zone Boundary Drop — COMPLETE

Implemented:

- Hostile mobs retain `AiComponent.targetEntityId` between ticks.
- If a target leaves the mob's room and is in an adjacent non-safe room, the mob moves one room toward the target.
- If the target's destination room is an effective safe zone, the mob drops the target and does not cross the boundary.
- Pursuit uses room exits through the World service lookup and does not attack on the same tick as movement.
- Physical movement and navigation handlers sync active player ECS `PositionComponent` values after successful WorldService movement so pursuit observes real runtime movement.

Deferred:

- Patrol route expansion for YELLOW/RED alert.

---

## Slice 3 — Bounded Multi-Room Pursuit — COMPLETE

Implemented:

- Hostile mobs with an existing target can path through connected non-safe rooms instead of only following adjacent targets.
- Pursuit moves one room per AI tick toward the target; it does not teleport or attack on the same tick as movement.
- Pursuit searches up to eight room transitions per AI tick and treats farther targets as lost scent to keep heartbeat work bounded.
- Pursuit does not path through intermediate effective safe-zone rooms.
- Room lookup continues to use the World service seam; exit values must resolve to matching room slugs so AI pursuit mirrors production movement semantics instead of accepting id-valued exits.

Deferred:

- Alert-expanded patrol routes for YELLOW/RED alert.

Follow-on completed (2026-08-02):

- Room-local terminal output now reports autonomous attacks and body-guard interceptions as combat messages.
- Pursuit departure and arrival messages are published to the affected physical rooms.
- Realtime output failures do not interrupt attacks or movement.

---

## Test Plan

- [x] Hostile mobs attack players in the same non-safe physical room.
- [x] Hostile mobs do not attack in effective safe zones.
- [x] Hostile mobs target jacked-in deckers by physical body room.
- [x] AP-starved hostile mobs recover and can attack again later.
- [x] Security reinforcements spawn as hostile AI.
- [x] Hostile mobs follow an existing target into an adjacent non-safe room.
- [x] Hostile mobs move one room along a path toward an existing target multiple rooms away.
- [x] Hostile mobs drop their target instead of crossing into an effective safe zone.
- [x] Hostile mobs drop targets beyond the bounded eight-transition pursuit search range.
- [x] Hostile mobs do not path through intermediate safe-zone rooms during pursuit.
- [x] Hostile mobs do not pursue through exits that only resolve as room ids.
- [x] Physical movement syncs the active player ECS position used by mob pursuit.

## Verification Commands

```bash
npx jest --runInBand tests/engine/mob-ai-system.test.ts tests/engine/combat-system.test.ts tests/engine/ecs.test.ts tests/engine/combat-ecs.test.ts tests/mission/mission.service.test.ts --silent
npm run build
```
